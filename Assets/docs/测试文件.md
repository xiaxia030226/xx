QFramework使用：UIKitConfig以及UIPanel预制体的创建流程

```c#
public class GameUIKitConfig : UIKitConfig
{
    private const string PanelPathPrefix = "UI/";

    public override IPanel LoadPanel(PanelSearchKeys panelSearchKeys)
    {
        FillPrefabPath(panelSearchKeys);
        return base.LoadPanel(panelSearchKeys);
    }

    public override void LoadPanelAsync(PanelSearchKeys panelSearchKeys, System.Action<IPanel> onPanelLoad)
    {
        FillPrefabPath(panelSearchKeys);
        base.LoadPanelAsync(panelSearchKeys, onPanelLoad);
    }

    private static void FillPrefabPath(PanelSearchKeys panelSearchKeys)
    {
        if (panelSearchKeys == null || panelSearchKeys.PanelType == null) return;
        if (!string.IsNullOrEmpty(panelSearchKeys.GameObjName)) return;

        panelSearchKeys.GameObjName = PanelPathPrefix + panelSearchKeys.PanelType.Name;
    }
}
```

我的UI预制体UITest路径为“Asset/Resources/UI/UITest.prefab”，都放在UI文件夹下面

那么需要设置PanelSearchKeys的GameObjName为路径名，“UI/UITest”



UI预制体的创建流程：

1.搭好UI预制体，

![image-20260826180621608](C:\Users\LF\AppData\Roaming\Typora\typora-user-images\image-20260826180621608.png)

右键选择@UIKit，会自动生成和预制体名字一样的cs文件

2.对于需要作为参数的内容（比如图片、按钮），挂上Bind脚本

![image-20260826180752136](C:\Users\LF\AppData\Roaming\Typora\typora-user-images\image-20260826180752136.png)

3.把生成的cs文件挂到预制体上面，点击“生成代码”

![image-20260826180841740](C:\Users\LF\AppData\Roaming\Typora\typora-user-images\image-20260826180841740.png)



UIPanel的通用方法：

```c#
protected override void OnInit(IUIData uiData = null)
protected override void OnOpen(IUIData uiData = null)
protected override void OnShow()
protected override void OnHide()
protected override void OnClose()
```

首次创建时：OnInit,OnOpen,OnShow

每次打开时：OnOpen,OnShow

每次关闭时：OnHide,OnClose(会销毁预制体从UIKit注册表中移出)